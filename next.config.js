/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Se você tinha alguma configuração real dentro do objeto,
  // certifique-se de copiá-la para cá.
  // Por exemplo: reactStrictMode: true, etc.
};

module.exports = nextConfig;